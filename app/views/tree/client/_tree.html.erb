<% unless tree.blank? %>
<i class='render_tree' style='display:none' data-locale="<%= t(:tree).to_json %>" data-klass="<%= opts[:klass] %>" data-plural="<%= opts[:klass].pluralize %>" data-rebuild-url="<%= url_for(controller: opts[:klass].pluralize, action: :rebuild) %>">
<i class='data'><%= tree.to_json %></i>
</i>
<% end %>